module.exports = {
  createUsersTable: 'CREATE TABLE IF NOT EXISTS users (id INT NOT NULL AUTO_INCREMENT, username TEXT NOT NULL, password TEXT NOT NULL, role TEXT NOT NULL, PRIMARY KEY (id))',

  
};